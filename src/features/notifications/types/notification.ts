export type Notification = {
  id: string;
  messageId: string;
  createdAt: string;
  documentId: string;
  name: string;
  companyName: string;
  companySlug: string;
  issuer: string;
  dueDate: string;
};
